export enum STAGE {
   DEVELOPMENT = "Development",
   PRODUCTION = "Production",
   TEST = "Test",
   NONE = "None"
}

export enum NOW_STAGE {
   STAGING = "staging",
   PRODUCTION = "production",
   NONE = "none"
}
